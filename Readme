
----------------------------------------------
       To create & activate virtual env
----------------------------------------------
Run in terminal
python -m venv myvenv

To activate virtual env
Run in terminal
myvenv/Scripts/activate

-----------------------------------------------
       To create & install requirements.txt
-----------------------------------------------
Install in venv
pip install pipreqsnb

Run in venv 
pipreqsnb --force .\CompareSourcefiles.ipynb

To install requirements on new machine
pip install -r requirements.txt
